<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Colored Strip -->
        <View
            android:id="@+id/view_strip"
            android:layout_width="6dp"
            android:layout_height="0dp"
            android:background="@color/pastel_blue_bg"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_subject_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            android:text="Lập trình Web"
            android:textColor="@color/text_title"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/view_strip"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Teacher -->
        <ImageView
            android:id="@+id/ic_teacher"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginTop="12dp"
            android:src="@drawable/ic_user_info"
            app:tint="@color/text_subtitle"
            app:layout_constraintStart_toStartOf="@id/tv_subject_name"
            app:layout_constraintTop_toBottomOf="@id/tv_subject_name" />

        <TextView
            android:id="@+id/tv_teacher"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="16dp"
            android:text="ThS. Nguyễn Văn A"
            android:textColor="@color/text_subtitle"
            android:textSize="14sp"
            app:layout_constraintBottom_toBottomOf="@id/ic_teacher"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/ic_teacher"
            app:layout_constraintTop_toTopOf="@id/ic_teacher" />

        <!-- Room -->
        <ImageView
            android:id="@+id/ic_room"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginTop="8dp"
            android:src="@drawable/ic_location"
            app:tint="@color/text_subtitle"
            app:layout_constraintStart_toStartOf="@id/tv_subject_name"
            app:layout_constraintTop_toBottomOf="@id/ic_teacher" />

        <TextView
            android:id="@+id/tv_room"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="16dp"
            android:text="Phòng A2-304"
            android:textColor="@color/text_subtitle"
            android:textSize="14sp"
            app:layout_constraintBottom_toBottomOf="@id/ic_room"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/ic_room"
            app:layout_constraintTop_toTopOf="@id/ic_room" />

        <!-- Schedule -->
        <ImageView
            android:id="@+id/ic_time"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_time"
            app:tint="@color/text_subtitle"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@id/tv_subject_name"
            app:layout_constraintTop_toBottomOf="@id/ic_room" />

        <TextView
            android:id="@+id/tv_schedule"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:text="T2 (7:00 - 11:30)"
            android:textColor="@color/text_subtitle"
            android:textSize="14sp"
            android:lineSpacingExtra="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/ic_time"
            app:layout_constraintTop_toTopOf="@id/ic_time" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
